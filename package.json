{
  "name": "@mattb.tech/octio",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "graphql",
    "ingest-consumption",
    "ingest-grid",
    "gap-checker-consumption",
    "cdk",
    "transform-data",
    "snapshot-tests"
  ],
  "scripts": {
    "deploy": "yarn workspace @mattb.tech/octio-cdk run deploy",
    "build": "yarn workspaces run build",
    "format": "yarn workspaces run format",
    "format:check": "yarn workspaces run format:check"
  }
}
